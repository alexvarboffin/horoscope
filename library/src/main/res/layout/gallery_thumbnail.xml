<?xml version="1.0" encoding="utf-8"?>
<com.walhalla.horolib.ui.helper.SquareLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#939090"
    android:orientation="vertical">

    <!--android:scaleType="centerCrop" -png-->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/tv_name">

        <ImageView
            android:id="@+id/thumbnail"
            android:layout_width="110dp"
            android:layout_height="wrap_content"

            android:layout_centerInParent="true"
            android:layout_margin="@dimen/activity_horizontal_margin"
            android:adjustViewBounds="true"
            android:contentDescription="@string/app_name"
            app:srcCompat="@drawable/ic_aries" />
    </RelativeLayout>

    <!--<ImageView-->
    <!--android:id="@+id/overflow_menu"-->
    <!--android:layout_width="@dimen/ic_album_overflow_width"-->
    <!--android:layout_height="@dimen/ic_album_overflow_height"-->
    <!--android:layout_alignParentEnd="true"-->
    <!--android:layout_alignParentRight="true"-->
    <!--android:layout_marginTop="@dimen/ic_album_overflow_margin_top"-->
    <!--android:contentDescription="@+id/overflow_menu"-->
    <!--android:scaleType="centerCrop"-->
    <!--android:visibility="gone"-->
    <!--android:src="@drawable/ic_dots" />-->


    <TextView
        android:id="@+id/tv_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="?attr/colorPrimary"
        android:gravity="center_horizontal"
        android:maxLines="1"
        android:padding="0dp"
        android:textAlignment="center"
        android:textAppearance="@style/Base.TextAppearance.AppCompat.Medium"
        android:textColor="@android:color/white"
        tools:text="Aries" />
</com.walhalla.horolib.ui.helper.SquareLayout>